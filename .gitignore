node_modules/
*.db
*.db-shm
*.db-wal
.env
.env.*.local
.styxx-treasury.*
.darkcoin-treasury.*

# Agent key backups â€” NEVER commit these
agent-keys-backup-*.json
keys-backup.json
keys-emergency.json
keys-*.json

# local agent credentials (dogfood)
.fable-agent.local
*.agent.local

# every local-only file (env, tokens, agent creds) — never commit
*.local
.admin-token.local
